# Where contact-form submissions are delivered.
#
# Required. Without it /api/kapcsolat rejects submissions with `no_sink` and the
# form tells the visitor to e-mail directly — deliberately, so a lead is never
# silently lost. Point this at a CRM intake endpoint, an automation hook, or an
# internal service that accepts a JSON POST.
EZEKIEL_LEAD_WEBHOOK=
